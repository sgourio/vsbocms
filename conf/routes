# This file defines all module routes (Higher priority routes first)
#
# import these routes in the main app as :
# *     /                       module:vsbocms
#
# ~~~~

GET     /?                     Module.index
*	  /{controller}/{action}/{fatherNodeId}/{treeNodeId}   vsbocms.{controller}.{action}
*	  /{controller}/{action}/{fatherNodeId}   vsbocms.{controller}.{action}
*     /{controller}/{action}   vsbocms.{controller}.{action}